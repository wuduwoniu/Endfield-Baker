# 庄方宜模拟器 V1.00 项目简介

## 线上地址
**https://deepseek-chat-clone.vercel.app**

---

## 一、项目概述

基于 React 19 + Vite 8 构建的 AI 多角色聊天应用，模拟《明日方舟：终末地》角色对话。采用 Baker-DX 暗色工业风格 UI（背景色 `#0a0b10`），通过 DeepSeek API 驱动角色扮演对话。

### 核心体验
- 选择不同角色开启对话，每个角色有独立人设
- AI 流式回复（逐字输出），支持 Markdown 渲染
- 38 个 Emoji 表情包系统，点击发送表情，AI 理解情绪含义
- 168 张表情贴图（Emoji/贴图双标签切换），贴图独立成消息，文字+贴图混合发送
- Baker 风格 UI 完美还原（聊天头、装饰边框、头像框、SVG 尾巴）

---

## 二、技术架构

### 四层架构（单向依赖）

```
components/hooks  →  store  →  api  →  config
                    store  →  utils
                    api    →  config
```

| 层级 | 目录 | 职责 | 依赖 |
|------|------|------|------|
| 配置层 | `src/config/` | API 地址、模型定义、UI 文本、表情映射 | 零依赖 |
| 通信层 | `src/api/` | 封装 DeepSeek API 调用和 SSE 流解析 | 只依赖 config |
| 工具层 | `src/utils/` | 纯函数：ID 生成、时间格式化、文本截断 | 零依赖 |
| 状态层 | `src/store/` | Zustand 管理聊天状态、角色人设、流式控制 | 依赖 api + utils |
| UI 层 | `src/components/` | 所有界面组件 | 主要通过 store selector 获取数据 |
| Hooks | `src/hooks/` | 主题切换 Hook | 独立 |

### 组件数据获取规范

- **运行时状态**（消息列表、流式状态等）→ 必须通过 store selector
- **静态常量**（UI 文本、表情映射表等）→ 允许直接从 config import
- **禁止** 组件直接 import api/ 或 utils/

---

## 三、安全架构

API Key 永不进入前端 bundle：

```
开发环境              Vite proxy (loadEnv) 注入 Key → 代理到 DeepSeek API
生产环境 (Vercel)     Serverless Function (api/deepseek.js) 注入 Key → 转发 DeepSeek API
前端发请求            fetch /api/deepseek/chat/completions（无 Authorization header）
```

---

## 四、功能模块

### 4.1 多角色系统

三名可对话角色，侧边栏点击切换：

切换角色时自动清空消息并注入对应人设提示词（system message）。

### 4.2 表情包系统

**入口**：输入框右侧表情按钮（`chat_emoji.png`）

**弹窗**：
- 底部居中弹出，输入栏正上方，灰色背景 `rgb(220,220,220)`
- 左侧竖排双标签切换：Emoji / 贴图，选中高亮
- Emoji 标签：8 列网格，2 行可见，上下滑动 38 个表情
- 贴图标签：5 列网格，2 行可见，上下滑动 168 张贴图
- 所有按钮用 `onMouseDown + preventDefault` 防止键盘聚焦，点击为唯一选择方式
- 点击遮罩或选择表情/贴图后自动关闭，输入框自动恢复聚焦

**Emoji 发送**：
- 点击 Emoji → 输入框插入 `[情绪]` 中文标记（如 `[开心]`）
- AI 收到情绪文字，理解含义
- 聊天记录中 `[情绪]` 渲染为内联表情图片

**贴图发送**：
- 输入框为空 + 选贴图 → 面板关闭，贴图直接发出，AI 回复随机贴图
- 有文字 + 选贴图 → 面板关闭，贴图加入队列，输入框内显示缩略图（可 × 删除）
- 发送时按序执行：文字 → 队列贴图逐张 → AI 回复（贴图不发给 AI）
- 贴图消息独立气泡，120×120 大图，无 SVG 尾巴

**素材**：38 个 Emoji PNG + 168 张贴图（game/ 120 PNG + skland/ 48 WEBP）

### 4.3 流式对话

- SSE 流式传输，AI 逐字输出
- 消息气泡支持 Markdown（react-markdown + GFM + 代码高亮）
- 流式进行中有闪烁光标指示
- 可随时停止流式生成

### 4.4 聊天头切换

ChatHeader 提供两套 Baker 风格聊天头装饰（Default / Alt），通过菜单按钮切换，默认使用 Alt 版。

### 4.5 主题切换

支持深色/浅色模式，状态持久化到 localStorage，通过 Header 中的日月图标切换。

### 4.6 新会话

侧边栏底部"添加新会话"按钮，一键清空当前对话。

---

## 五、文件结构

```
/
├── api/deepseek.js                  Vercel Serverless 代理
├── vercel.json                      路由重写配置
├── vite.config.js                   Vite 配置 + dev proxy
├── tailwind.config.js               暗色主题色板
├── vitest.config.js                 测试配置
├── CLAUDE.md                        项目说明书
├── LAYOUT_SPEC.md                   UI 排版规格文档
├── docs/
│   ├── V1.00-项目简介.md             本文件
│   └── superpowers/
│       └── specs/
│           └── 2026-05-05-emoji-completion-design.md  表情模块设计文档
├── public/baker-assets/             Baker-DX 素材
│   ├── avatars/                     角色头像 + 终端管理员
│   ├── backgrounds/                 HUD 背景 + 聊天纹理
│   ├── decorations/                 推文装饰条
│   ├── emojis/                      38 个表情 PNG
│   ├── stickers/                    168 张贴图 (game/ + skland/)
│   ├── header/                      聊天头素材 (2 套)
│   ├── icons/                       功能图标
│   ├── input/                       发送/加号/表情按钮图标
│   ├── masks/                       头像遮罩
│   └── ui/                          头像框 + 新会话图标
└── src/
    ├── App.jsx                      主布局
    ├── main.jsx                     入口
    ├── config/
    │   ├── index.js                 API 配置、模型定义、UI 文本
    │   ├── emojis.js               表情 key 定义、情绪映射、文本解析
    │   └── stickers.js             贴图 key 定义、路径映射
    ├── api/
    │   └── index.js                DeepSeek API 封装 + SSE 流解析
    ├── store/
    │   └── chatStore.js            Zustand 状态管理
    ├── utils/
    │   └── index.js                纯工具函数
    ├── hooks/
    │   └── useTheme.js             主题切换 Hook
    ├── styles/
    │   └── index.css               Tailwind 指令 + CSS 变量
    └── components/
        ├── CharacterContext.jsx     多角色 Context
        ├── characters.js            角色定义
        ├── Header/                  Header.jsx, Logo.jsx, ModeSelector.jsx, ThemeToggle.jsx
        ├── Sidebar/                 Sidebar.jsx, HistoryList.jsx, NewChatButton.jsx
        ├── Chat/                    ChatArea.jsx, ChatHeader.jsx, MessageList.jsx, MessageBubble.jsx, MarkdownContent.jsx, WelcomeScreen.jsx
        └── Input/                   ChatInput.jsx, StickerPanel.jsx
```

---

## 六、Zustand 状态

| 字段 | 类型 | 说明 |
|------|------|------|
| `messages` | `Array<{id, role, content, contentType?, timestamp}>` | 消息列表（contentType 可选 'sticker'） |
| `currentMode` | `string` | 当前模型模式 (fast/expert/vision) |
| `currentPrompt` | `string \| null` | 当前角色人设提示词 |
| `isStreaming` | `boolean` | 是否流式传输中 |
| `error` | `string \| null` | 错误信息 |
| `abortController` | `AbortController \| null` | 流式中断控制器 |

**Actions**：`sendMessage()`、`addUserText()`、`triggerAiReply()`、`sendSticker()`、`sendStickerReply()`、`setMode()`、`setPrompt()`、`clearMessages()`、`stopStream()`、`appendChunk()`、`finishStream()`

---

## 七、数据流（发送消息）

```
用户输入文字 → ChatInput.handleSend()
  → 首次消息时 store.setPrompt(character.prompt)      # 注入角色人设
  → store.sendMessage(text)
    → 过滤空占位消息，构建 historyMessages
    → 若有 currentPrompt，插入 system message
    → api.sendChatMessage(historyMessages)
      → POST /api/deepseek/chat/completions
      → 【本地】Vite proxy 注入 Key
      → 【线上】Vercel Serverless 注入 Key
    → parseSSEStream(response.body)                   # 逐 chunk 解析
    → store.appendChunk() → finishStream()
    → MessageBubble 实时显示 AI 回复

贴图发送流程：
用户选贴图 → 无文字：sendSticker() + sendStickerReply()（AI 随机贴图）
           → 有文字：加入 stickerQueue → 发送时 addUserText → sendSticker → triggerAiReply
```

---

## 八、技术栈

| 类别 | 技术 |
|------|------|
| 框架 | React 19 + Vite 8 |
| 样式 | Tailwind CSS 3（自定义 Baker 暗色色板） |
| 状态 | Zustand 5 |
| Markdown | react-markdown + rehype-highlight + remark-gfm |
| API | DeepSeek API (OpenAI 兼容), SSE 流式 |
| 测试 | Vitest + @testing-library/react + jsdom（56 个用例） |
| 部署 | Vercel (Serverless Function 代理) |
| 仓库 | github.com/wuduwoniu/Zhuang-Fangyi |

---

## 九、测试

| 测试文件 | 用例数 | 覆盖内容 |
|----------|--------|----------|
| `config/emojis.test.js` | 15 | 表情 key 数量/去重、图片路径映射、情绪文本映射、文本解析 |
| `config/stickers.test.js` | 7 | 贴图 key 数量、game/skland 路径映射、去重 |
| `api/index.test.js` | 4 | 请求体构建、SSE 流解析 |
| `store/chatStore.test.js` | 7 | 初始状态、模式切换、角色人设、清空、发送消息、贴图发送、贴图 AI 回复 |
| `StickerPanel.test.jsx` | 7 | 双标签渲染、Emoji/贴图网格切换、选择回调、遮罩关闭 |
| `MessageBubble.test.jsx` | 3 | 用户/AI 气泡渲染、贴图消息渲染 |
| `ChatInput.test.jsx` | 2 | 输入框和按钮渲染、空输入时隐藏发送 |
| `ChatArea.test.jsx` | 2 | 空状态/有消息状态 |
| `Header.test.jsx` | 2 | Baker 路径、玩家信息 |
| `ThemeToggle.test.jsx` | 3 | 图标切换、回调 |
| `Sidebar.test.jsx` | 1 | 联系人列表渲染 |
| `App.test.jsx` | 1 | 主布局冒烟测试 |
| **合计 12 文件** | **56** | 全部通过 |

---

## 十、版本记录

| 版本 | 日期 | 内容 |
|------|------|------|
| V1.00 | 2026-05-05 | 多角色对话、流式 AI、Baker 暗色 UI、38 表情包系统、深色/浅色主题、Vercel 安全部署 |
| V1.10 | 2026-05-05 | 168 张贴图系统、Emoji/贴图双标签面板、贴图混合发送、AI 贴图回复 |

---

> 项目说明书：CLAUDE.md | UI 排版规格：LAYOUT_SPEC.md | 表情模块设计：docs/superpowers/specs/2026-05-05-emoji-completion-design.md
